# Nome do seu assistente/bot
BOT_NAME="Assistant"

# Label da sessão OpenClaw que o servidor vai acionar
# (geralmente "main" para a sessão principal)
OPENCLAW_SESSION_LABEL="main"

# API Key para autenticação
# Gere uma com: openssl rand -base64 32
MCP_SERVER_API_KEY="your-api-key-here"

# Porta do servidor HTTP
PORT=3721

# Bind address (0.0.0.0 para aceitar conexões externas)
# Use 127.0.0.1 se quiser apenas localhost
BIND_ADDRESS="0.0.0.0"

# Caminho do workspace OpenClaw (onde está MEMORY.md)
OPENCLAW_WORKSPACE="${HOME}/.openclaw/workspace"
